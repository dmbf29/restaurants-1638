<div class="container">
  <h1>Add Review for <%= @restaurant.name %></h1>
  <!-- add the action ( post /restaurants/22/reviews) -->
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.submit class: 'btn btn-primary mt-2' %>
  <% end %>
</div>
